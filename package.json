{
  "name": "aug2026-grok-bot",
  "version": "1.0.0",
  "description": "A minimal, self-contained chatbot web app powered by the xAI Grok API, with an offline demo mode.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
