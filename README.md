# Workspace

## Grasshopper Run

Playable 32-bit side-scroller in [`grasshopper-run/`](grasshopper-run/).

```bash
cd grasshopper-run && python3 -m http.server 8080
```

Open http://localhost:8080 — Space to jump, M to shoot fire, reach the finish 10 screens away.
